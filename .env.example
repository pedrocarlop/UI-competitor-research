# Browser automation (optional — for automated screenshot capture)
BROWSER_AGENT_COMMAND=
PLAYWRIGHT_WS_ENDPOINT=
HEADLESS=true

# Figma (optional — only if exporting to Figma)
FIGMA_ACCESS_TOKEN=
FIGMA_API_TOKEN=
FIGMA_WRITE_COMMAND=
